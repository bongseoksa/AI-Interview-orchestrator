# 03 — 로드맵 · 리스크 · 미결정

## 1. 단계별 마일스톤

각 마일스톤은 독립적으로 "보여줄 수 있는" 산출물을 남긴다.

### GM0 — 이벤트 백본 (기반)
- `src/gui/events.py`, `listener.py`, `bus.py` 구현
- `GUIEventListener`를 이벤트 버스에 등록 → 콘솔로 OfficeEvent(JSON) 출력 검증
- **DoD**: 아무 Crew나 실행하면 구조화 이벤트가 실시간으로 찍힌다 (`crew_logger`와 병존)

### GM1 — 로컬 웹 대시보드 (관전)
- FastAPI 서버 + `/ws/office` + `/api/agents` + `/api/crews`
- 프론트 MVP: 11개 책상 + 상태 아이콘 + 회의록 피드 (DOM/CSS)
- `main.py gui` 명령으로 기동 → 브라우저 자동 오픈
- **프로세스 경계 주의**(02 §8-1): GUI 서버와 CLI 크루는 별도 프로세스라 이벤트 버스를 공유하지
  않는다. GM1은 **사이드카/테일 모드**(크루가 남긴 `sessions/<run_id>.jsonl`을 tail)로 관전한다.
- **DoD**: 터미널에서 `python main.py architect` 실행 시(세션 JSONL 기록됨), 브라우저 사무실에서
  아키텍트 팀이 일하는 모습이 실시간으로 보인다 (GUI 버튼 실행=인프로세스는 GM2)

### GM2 — 통제 + 산출물 (상호작용)
- `POST /api/run/{crew}` + 통제 바 버튼 (codegen/notion-edit 인자 모달)
- 산출물 선반(`output/` 목록·미리보기), 직원 프로필 카드
- 세션 상태·경과·토큰 카운터
- **DoD**: 사무실 UI 버튼만으로 Crew를 실행하고 산출물까지 열람한다

### GM3 — 사무실 연출 고도화 (몰입)
- 아바타·이동·도구 애니메이션, 회의실 클로즈업 (필요 시 Phaser)
- LLM 스트리밍 자막(옵션), 사운드(옵션), 픽셀/대시보드 뷰 토글
- **DoD**: 비개발자가 봐도 "직원들이 일하는 사무실"로 읽힌다

### GM4 — 데스크톱 패키징 (배포)
- PyWebView 셸로 백엔드+웹뷰 단일 프로세스화
- PyInstaller로 macOS `.app` / Windows `.exe` 빌드, 실행 가이드
- **DoD**: Mac·Windows에서 더블클릭 한 번으로 사무실 앱이 뜬다

### GM5 — 사람 확인함 (Human Review) — [`05`](05-human-review-and-docs.md) §A
- 세션 이벤트에서 확인함 항목 자동 파생: 노션 초안 승인(유형1), 오류 조치(유형5)
- 확인함 패널 + 뱃지, 승인/반려/보류, 노션 초안 **dry-run → 승인 → `sync_notion` 반영**
- 영속 스토어(`gui_data/review.db`)
- **DoD**: 서기가 만든 노션 초안을 확인함에서 미리보고 승인하면 노션에 반영된다
- **(고도화)** CrewAI `human_input` 라우팅(유형4, 실행 블로킹 질문), 할루시네이션 플래그(유형6)

### GM6 — 문서·회의록 조회 — [`05`](05-human-review-and-docs.md) §B
- `listener`가 세션 구조화 이벤트를 `gui_data/sessions/<run_id>.jsonl`에 적재
- 작업결과문서 뷰어(마크다운/코드 + 메타 + "만든 회의로 가기")
- 회의 목록(일자+Crew+주제, 검색) + 트랜스크립트(원본 로그 점프)
- **DoD**: 과거 실행을 "회의"로 열어 에이전트 논의 전 과정을 읽고, 산출물을 조회한다
- **(고도화)** 회의 리플레이(사무실 재연 + 타임라인 스크럽)

## 2. 의존성·순서

```
GM0 ──▶ GM1 ──▶ GM2 ──▶ GM3
                  ├──────▶ GM4 (GM2 이후 언제든 병행)
                  ├──────▶ GM5 확인함  (GM1 이벤트 백본 위에서 동작)
                  └──────▶ GM6 문서·회의록 (세션 JSONL 적재는 GM0/GM1과 함께 넣으면 유리)
```

GM0→GM1이 가치의 80%. GM3(연출)·GM4(패키징)·GM5(확인함)·GM6(조회)는 우선순위에 따라 순서 교체 가능.
단, **GM6의 세션 JSONL 적재는 GM0 리스너 구현 시 함께 넣어두면** 나중에 과거 회의를 소급 복원할 수 있어 유리하다.

## 3. 리스크 · 대응

| 리스크 | 영향 | 대응 |
|--------|------|------|
| CrewAI 이벤트 필드가 버전마다 다름 | 리스너 파싱 깨짐 | `getattr` 방어적 접근(기존 로거와 동일 패턴), 이벤트 스냅샷 테스트 |
| kickoff 블로킹으로 서버 멈춤 | UI 프리즈 | worker thread 실행, 이벤트 버스 전역 수신 활용 |
| LLM 스트리밍 이벤트 I/O 과부하 | 프론트 렉 | `design-crew-logger.md §3.4` 버퍼링(문장/500자 flush) 재사용, 스트림은 옵션 |
| PyInstaller가 CrewAI/Ollama 의존성 누락 | 빌드 실패 | hidden-import 목록 관리, GM4 초반에 최소 빌드 스파이크 선행 |
| 게임형 연출 과투자 | 일정 지연 | GM1(정보 전달) 먼저 확정 후 GM3 연출은 선택적 |
| Ollama 미기동 상태에서 실행 | Crew 실패 이벤트만 | UI에 "Ollama 오프라인" 배너 + 헬스체크 |
| **프로세스 경계 오해**(별도 터미널 크루 미수신) | GM1 관전 불가 | 02 §8-1 사이드카/테일 모드로 설계 확정, GM0에서 검증 |
| **스레드→asyncio 경계**(워커 스레드에서 WS 접근) | 브로드캐스트 깨짐/경쟁 | `call_soon_threadsafe` 계약(02 §8-2), GM0 스파이크에 포함 |
| **발화 300자 절단** | 속기록·트랜스크립트 충실도 저하 | **해소(D8)**: 저장·노션은 무절단(청킹 split), 표시만 절단. 리스너가 소스 원문 캡처 |

## 4. 미결정 사항 (사용자 확인 필요)

| # | 결정 | 권고 | 비고 (레퍼런스 근거는 [`04-references.md`](04-references.md)) |
|---|------|------|------|
| D1 | 데스크톱 셸 | PyWebView + PyInstaller | Python 단일 언어. **Tauri가 이 분야 표준**(AgentRoom·agents-in-the-office)이나 우리는 백엔드가 Python이라 사이드카 배선 필요 → JS 프론트 본격화 시 Tauri 격상 |
| D2 | 프론트 렌더링 | DOM/CSS MVP → 필요 시 Phaser | 저의존 대안: **agent-town식 Canvas2D**. 게임형 픽셀을 처음부터 원하면 Phaser(AgentOffice 실증) |
| D3 | 범위 | 관전 + 통제 | 관전 전용이면 GM2 축소 |
| D4 | 착수 방식 | 로컬 웹 MVP 먼저 | 처음부터 데스크톱 앱을 원하면 GM4를 앞으로 |
| D5 | 시각 톤 | 스타일라이즈드(깔끔) | 사용자 원본 레퍼런스(DeskRPG)는 **픽셀 도트 멀티플레이 감성**. 픽셀 vs 미니멀 대시보드 |
| D6 | 사운드/알림 | 옵션(기본 OFF) | 완료·오류 사운드 넣을지 |
| D7 | 참고 코드 활용 | MIT 사례 학습·부분 차용 | AgentOffice(Ollama 어댑터)·agent-town(저의존 캔버스) 코드 참고. 에셋 라이선스 별도 확인 |
| D8 | 발화 무절단 캡처 | **채택** — 3계층(표시=절단 / 저장·노션=무절단) | 절단은 표시용일 뿐. 노션 청킹은 split이라 손실 0. 소스(리스너)에서 원문 캡처 필수 (02 §8-5) |

## 5. 레포 원칙과의 정합

`CLAUDE.md`의 개발 역할 분담 원칙에 따라, **구현 착수 전 다음 Crew 실행을 권장**한다:

1. **ArchitectCrew** — 이벤트 파이프라인·모듈 구조 검증 (`02-architecture.md`를 입력으로)
2. **FrontendCrew** — 사무실 UI 컴포넌트·상태 머신 상세 설계 (`01-vision-and-metaphor.md` 입력)
3. **QACrew** — 이벤트 리스너·WebSocket 브로드캐스트 테스트 전략
4. **ReviewCrew(외부인사)** — "이 GUI가 실사용될 가치가 있는가" 냉정한 검증

이 `docs/gui/` 문서 자체가 위 Crew들에 넘길 **설계 초안(input)** 역할을 한다.
Notion 반영이 필요하면 **서기에이전트(DocumentationCrew)** 경유 원칙을 따른다.

## 6. 다음 액션 (제안)

- [ ] D1~D8 결정 확정
- [ ] **GM0 스파이크(02 §8 검증 포함)**: `GUIEventListener`로 이벤트 JSON 콘솔 출력 +
  세션 JSONL 적재(무절단 옵션 D8) + **스레드→asyncio 핸드오프(§8-2)** + **테일 모드(§8-1)** 실증 (1일)
- [ ] ArchitectCrew·FrontendCrew 설계 검증 실행 (확인함·회의록 뷰어 포함)
- [ ] GM1 착수 → 이후 GM5(확인함)·GM6(조회) 우선순위 결정
